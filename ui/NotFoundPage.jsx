/** @jsx React.DOM */

var React = require("react");

var NotFoundPage = React.createClass({
	propTypes: {
	},

	render: function render() {
		return (
			<p>Empty not found page</p>
		);
	}
});


module.exports = NotFoundPage;
